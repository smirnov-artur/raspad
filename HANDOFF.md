# HANDOFF — РАСПАД / DECAY (Three.js шоукейс для форума)

Контекст для публикующего терминала. Это НЕ продающий кейс — это вау-демо для форумов
(three.js forum showcase, реддиты, телеграм-чаты): «чтобы все обоссались кипятком» (ТЗ Артура).
Конвейер: концепт-панель 5 линз → судья (победил РАСПАД: 9/9/7/7) → сборка → отладка в живом браузере.
**Панель финальных критиков ещё НЕ прогонялась** — см. «Что осталось».

## Что это
Машина агрегатных состояний: 65 536–262 144 частицы проживают полный цикл материи.
Из пыли самособирается брутальное здание (каждый цикл — НОВОЕ, seed меняется; первый цикл
собирает слово РАСПАД/DECAY по языку страницы) → курсор греет бетон, по фасаду ползут
светящиеся трещины (Грей-Скотт 256², не круглое пятно, а ветвящаяся паутина) → срыв:
~200–1000 Вороной-кластеров рушатся твёрдыми кувыркающимися обломками (rigid-body ЦЕЛИКОМ
на GPU: интеграция кватернионов, контакт пола по 8 крайним точкам — ни грамма CPU-физики)
→ обломки плавятся в чёрную стеклянную лужу с раскалённой кромкой (screen-space метаболы)
→ испарение → газ спиралит в сингулярность-курсор (аккреционный диск с доплер-окраской)
→ имплозия, стоп-кадр, вспышка → время вспять: материя отстраивает НОВОЕ здание. Цикл ~60с.

Ввод: курсор = сингулярность (греет), колесо/щипок = bullet-time 0.05–1×, двойной клик =
следующий акт, наклон телефона = вектор гравитации в обрушении. 8с без ввода — автопилот
(демо играет само, идеально для записи гифки). Звук — полный WebAudio-синтез без единого
файла (дрон, треск трещин, гул обрушения через процедурный ConvolverNode, имплозия) —
включается кнопкой после первого клика (политика браузеров).

## Состав
- `index.html` RU / `en/index.html` EN (паттерн как в prolet: язык прибит `<html lang>`,
  перекрёстные ссылки в шапке, НИКАКОЙ кнопки-переключателя)
- `js/vendor/three.module.js` + `three.core.js` — r185 ЛОКАЛЬНО (core обязателен — module его импортирует!)
- `js/geometry.js` — процедурный генератор (здание/слово, Вороной-кластеры, Morton) + `js/test-geometry.js`
  (`node --input-type=module -e "import('./js/test-geometry.js')"` из папки → «7/7 — геометрия по контракту»)
- `js/audio.js` — WebAudio-синтез (лимитер, кроссфейд-лупы шума, без утечек)
- `js/sim.js` — ping-pong FBO с MRT (свой, GPUComputationRenderer не умеет MRT)
- `js/shaders.js` — все GLSL строками; `js/main.js` — акты/камера/ввод/пост
- `css/style.css` — оверлей (обязателен `[hidden]{display:none!important}` — display:grid перебивает hidden)

## Дебаг-инструменты (query-флаги, в проде не мешают)
`?tele` — телеметрия в title (акт/время/fps/timeScale); `?fast` — сборка ×4; `?phist` — гистограмма
фаз частиц в title (переводит FBO в Float32); `?expose` — window.__R с внутренностями; `?act=`,
`?hot` — грелка в центр фасада; `?n=128|256|512` — форс тира; `?nosim/nocubes/nopoints/nofloor/nopost/...` —
бисекция стадий; `` ` `` — дебаг-оверлей (цифры 1-6 — акты, R — новый цикл, G — фикс-сид).

## Публикация
1. Репо `raspad` → Pages main/root. RU корень, EN — `/en/`.
2. Это НЕ карточка портфолио в первую очередь — это ФОРУМНЫЙ пост. Подача (из концепта):
   «one HTML + import map, ноль ассетов, ноль билда, вся физика на GPU — view-source приветствуется».
   Тред на three.js forum (Showcase), пост в телеграм-чаты вебщиков, твит с гифкой.
   Гифку писать с автопилота (8с не трогать мышь) — драматургия играет сама.
3. В портфолио — потом, отдельной карточкой «эксперименты» (RU+EN блербы напишу после критиков).

## Проверено (в живом Chrome на железе, 60 fps стабильно)
Полный цикл ×3 без деградации: сборка → пояс трещин с блумом → обрушение кластерами →
лужа с огненной кромкой → газ → аккреция (фаза 5: 250+ частиц) → реверс → НОВОЕ здание.
Фазовая гистограмма чистая (0 NaN за 3 цикла), кластеры без взрывов (990/1024 спят после
обрушения), bullet-time колесом работает (ts 1.00→0.07), EN-страница собирает DECAY.
Выученные баги: (1) шейдерные ветки `uAct > N` ломались актом 5 — реверс маскируется под
акт 0 (`shaderAct`); (2) gl_PointSize без перспективного фактора = точки в 1px; (3) камера
в актах 3–4 смотрела на курсор, который автопилот уводил в поле — сингулярность/сток
клампятся к радиусу сцены; (4) THREE.Clock в r185 депрекейтнут — ручной performance.now().

## Панель критиков ПРОЙДЕНА + волна правок (2026-08-12, поздний вечер)
Оценки: GPU-инженер 7, код 7, арт/UX 6.5 («ядро мирового уровня, постить рано»). Применено ВСЁ high и medium:
- **Плавление тотальное**: heat теперь модификатор скорости, а не потолка — к концу акта III
  плавится вся материя (жидкость 676 → 3874 частиц в сэмпле, аккреция 250 → 1136).
- **Кино-slow-mo на срыве**: акт II стартует с timeScale 0.32 и отпускает через 1.5с —
  кувыркающиеся куски видны без ручного колеса (колесо перебивает).
- **Смена тира** больше не телепортирует материю в origin (targetsFresh → matInit-посев).
- **Пропуски актов дабл-кликом** безопасны: кластеры освобождаются при uAct>2.5,
  недособранные частицы доснапливаются (uAssembleT=1.2 в setAct(1)), uImplode сбрасывается.
- **Тач**: щипок не триггерит дабл-тап (isPrimary + счётчик указателей), touch-action:none
  (iOS игнорирует maximum-scale), запрос разрешения гироскопа на первом касании.
- **Звук**: ручной мьют не перебивается кликами (userMuted), скрытая вкладка глушится
  (visibilitychange → ctx.suspend).
- **Пересвет убит**: soft-clip перед блум-порогом, вклад блума 1.2→0.95, кромка/спекуляр
  метаболов ужаты, аккреция без выжигания в белый (яркость 1.8→1.1, доплер-диапазон ±4).
- **Свет**: высотный AO (низ темнее), камера ниже и на 30% ближе в актах II-III.
- Грей-Скотт: ~120 итер/с независимо от герцовки монитора, честно замирает в bullet-time.
- Жизненный цикл: webglcontextlost → честный фолбэк; статический #nogl + noscript +
  детект import map для старых Safari; resize с дебаунсом; диспоз материалов и
  instanceMatrix при смене тира; реверс не ускоряется флагом ?fast; вспышка живёт ~1.5с.
- Автопилот: в актах III-IV сингулярность держится у центра сцены (материя в кадре),
  кольцо курсора гаснет, тихий прогрев здания уже через 3.5с бездействия.
- Мобайл: жидкость рисуется тлеющими каплями через points (uNoSplat) — материя не исчезает.
- ?act=N реализован (прыжок в акт для скриншотов), KeyR не задваивает генерацию.
Проверка после волны: 0 ошибок консоли, 60fps, фазовая гистограмма чистая, полный цикл ×2.

## Что осталось (сессия-сборщик передаёт)
1. Аккреционный диск подтверждён данными (1136 частиц, доплер в коде), но статичным скрином
   не пойман (короткий и динамичный) — посмотреть живьём; при желании удлинить акт IV с 6.4с до ~8с.
2. Звук не слушали (нет ушей у терминала) — Артуру прожать кнопку ЗВУК и оценить баланс.
3. Мобильный тир (128², тач, наклон, капли вместо метаболов) — прогнать на реальном телефоне.
4. og:image нет — кадр «башня с горящей консолью» (в этой сессии выглядел лучше всего) в превью.
5. Низкий приоритет от инженера: бенч-окно тира меряется на дешёвом акте 0 (риск чрезмерного
   апгрейда до ULTRA — страхует resolution scaler), контакт кластеров последовательный по 8 точкам.

## ПЕРЕДЕЛКА ПО ЗАМЫСЛУ (2026-08-13, правки Артура — важнее всего выше)
Артур забраковал середину: «плавление в чёрную жижу и всасывание в огоньки никто не поймёт»,
«нагрев ничего не решает», «кубики крутятся вокруг себя — бред», «огонь пиксельный».
Сделано:
- **ПРИЧИННОСТЬ**: кластер отваливается, когда прогрета ЕГО зона фасада (UV-атлас 2x2, четыре
  стороны раздельно, поле Грея-Скотта 512²) ЛИБО когда под ним рухнула опора (граф опор
  считается в geometry-загрузке: ближайший нижний кластер с пересечением по XZ). Акт II —
  только сценарный дожим остатков. Драматургию ведёт материя: акт срыва наступает, когда
  реально сорвалось >50% кластеров (readback 32x32 раз в ~0.4с).
- **АКТЫ ПЕРЕСОБРАНЫ** (5 вместо 6): 0 ПЫЛЬ → I ОГОНЬ → II СРЫВ → III ПЕПЕЛ → V ЗАНОВО.
  Расплав-метаболы, аккреционный диск, доплер и имплозия ВЫКИНУТЫ целиком (фазы 3 и 5 мертвы,
  акт 4 переадресуется на 5). История читается без слов: сгорело → рухнуло → пеплом взмыло →
  пепел собрал новое здание.
- **РАСТВОРЕНИЕ**: раскалённый воксель съёживается (scale ~ 1-heat) — материал выгорает в решето
  и лишь потом уходит пеплом; бетон сперва чернеет коркой.
- **ОГОНЬ**: фронт горения = скорость реакции A·B² (кромка горит, выгоревшее ядро гаснет),
  жар на гранях с мерцанием, живой point-light на стенах и полу, искры и дым из фронта.
- **ДЫМ**: отдельный слой Points с NormalBlending (аддитивный дым не бывает тёмным), клубы с
  шумовой кромкой и вращением, видимы только в свете пожара, тают с высотой.
- **ВРАЩЕНИЕ ОБЛОМКОВ** от пройденного пути, а не от времени: лёг — замер (была юла на месте).
- **ЗВУК СБОРКИ** (просьба Артура): audio.tinkle() — пентатонические пинги защёлкивания вокселей,
  плотность колоколом по прогрессу, высота растёт к финалу, разброс по стерео. НЕ ПРОСЛУШАН.
- Масштаб: десктоп стартует сразу на 262k (бенч теперь только понижает), здания выше (26–35 ед.).
Проверено: 0 ошибок консоли, 60fps, полный цикл. НЕ проверено: звук ушами, телефон.